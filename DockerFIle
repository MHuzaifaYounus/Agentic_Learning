# Step 1: Use an official Python base image
FROM python:3.13

# Step 2: Set working directory
WORKDIR /app

# Step 3: Copy all files from your project into the container
COPY . .

# Step 4: Install pip and dependencies
RUN pip install --upgrade pip
RUN pip install chainlit asyncpg openai-agents sqlalchemy python-dotenv

# Step 5: Expose port (Hugging Face uses port 7860)
EXPOSE 7860

# Step 6: Command to run your Chainlit app
CMD chainlit run main.py --host 0.0.0.0 --port 7860
